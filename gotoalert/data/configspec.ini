# Directory paths
FILE_PATH = string(default=path_not_set)
HTML_PATH = string(default=path_not_set)

# Filter parameters
IGNORE_ROLES = string_list(default=list('test', 'utility'))

# Database parameters
ON_GRID = boolean(default=True)
MIN_TILE_PROB = float(default=0.01)
MAX_TILES = integer(default=10)

# Slack bot parameters
ENABLE_SLACK = integer(default=0)
SLACK_BOT_NAME = string(default=slack_bot)
SLACK_BOT_TOKEN = string(default=slack_token)
SLACK_BOT_CHANNEL = string(default=slack_channel)
