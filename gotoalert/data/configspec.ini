# HTML webpage path
HTML_PATH = string(default=path_not_set)

# Filter parameters
IGNORE_ROLES = string_list(default=list('test', 'utility'))
MIN_GALACTIC_LATITUDE = float(default=8.0)
MIN_GALACTIC_DISTANCE = float(default=15.0)

# Database parameters
ON_GRID = boolean(default=True)
MIN_TILE_PROB = float(default=0.01)
MAX_TILES = integer(default=10)
VALID_DAYS = integer(default=4)
